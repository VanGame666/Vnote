# LC3 编解码修复:16k 麦克风 + 48k LC3 库,经 16k↔48k 重采样

## 根因
LC3 库 `libtlka_lc3p2024_48k_5ms.a` 只支持 48kHz(`tlka_lc3a_enc_get_size(16000)` 返回 21 = `LC3PLUS_SAMPLERATE_ERROR`,实测 `lc3 enc init ret=21`)。麦克风管线是 16kHz(`MIC_SAMPLING_RATE=16000`,每帧 80 样本)。LC3 被 16k 初始化 → init 失败 → bitrate 设不上(`set_bitrate ret=11` 连带)→ 每帧 `encode ret=5`(`BITRATE_UNSET_ERROR`)、`enc` 全 0。

仓库内无 16k LC3 库;`LC3_WAVE_FORMAT` 之前被改成 INT16 也无济于事(根因是采样率)。

## 方案
保持 16k 麦克风管线;LC3 编解码按 48k 运行;TX 编码前 16k→48k 重采样,RX 解码后 48k→16k 重采样。重采样用 SDK 自带 `my_resample16to48_*` / `my_resample48to16_*`(实现在 `libmic_TL721X_1t4r_tx.a`/`_rx.a`,16k↔48k 为精确 3:1 整数比,输出 80→240、240→80 确定)。LC3 实例/scratch 缓冲按注释已是 48k 尺寸(`lc3p_enc_buff[2575]`、scratch 9792、`lc3p_dec_buff[5695]`、dec scratch 15600),init 有 throw 保护。

## 分阶段实施

### Stage 0 — 验证 48k LC3 接受配置(小改动排雷,先确认码率不牵连 RF)
码率若在 48k 非法会牵连帧长→RF 协议,故先单独确认。
- `app_audio_common.c`:编/解码 init 的 `samplerate` 用 48000(新增宏 `LC3_CODEC_SR 48000`)替代 `MIC_SAMPLING_RATE`。
- TX task 临时跳过 `app_audio_lc3p_encode_frame_int16` 调用(避免 48k 编码器读 80 样本越界),只让 init 跑。
- 验证启动日志:`lc3 enc init ret=0`、`lc3 enc set_bitrate(48000) ret=0`,且不抛 `lc3p_enc_buff not enough` / `enc_scratch_size not enough`。
- 若 `set_bitrate ret=3`(`BITRATE_ERROR`)→ 48kbps 在 48k 非法,暂停,重议码率/帧长/RF。

### Stage 1 — TX:16k→48k 重采样 + 编码
- `app_audio_common.h`:`LC3_WAVE_FORMAT` 改回 `LC3_WAV_FORMAT_INT24`(匹配 24bit 管线与重采样 int 输出)。
- `app_audio_1t4r_uni_tx.c` init:调用 `my_resample16to48_init()`(必要时 `my_resample16to48_set_buffer`)。
- TX task:读 80 样本(16k)→ `get_pcm16_from_pcm16x2` → `pcm16_to_pcm24` → `my_resample16to48_data(mono16k, 80, mono48k, 0)`(得 240)→ `app_audio_lc3p_encode_frame_int24((int*)mono48k, enc)` → `app_audio_uart_tx_push_data(enc, enc_size)` + `talmll_encode_compete(0, enc, enc_size)`。无 I2S 数据分支:对清零的 80 样本走同一重采样+编码路径(产出静音帧),保持 RF/UART 连续。
- 新缓冲:`pcm24_int mono48k[240 + 12]`。
- 验证:`LC3 encode ret=0`,UART `enc` 30B 非 0。
- 链接风险:若报 `my_resample16to48_*` undefined → 重采样符号不可用,回退重议(可能要自写 3x 重采样或换 48k 管线)。

### Stage 2 — RX:解码 + 48k→16k 重采样
- `app_audio_1t4r_uni_rx.c` init:调用 `my_resample48to16_init()`。
- RX task:`app_audio_lc3p_decode_frame_int24(sel, enc_buf, pcm48k, plc)`(得 240)→ `my_resample48to16_data(pcm48k, 240, pcm16k, 0)`(得 80)→ 输出 80 样本(UART/DAC)。顺带修掉 `short pcm_out` 传 `int*` 的类型不匹配与 `ppcm` 死代码:解码写入 int 缓冲。
- 新缓冲:`pcm24_int pcm_dec48k[240 + 12]`、`pcm_out16k[80 + 12]`。
- PLC 路径:`plc=1` 时解码器做 concealment(48k 240 样本)再重采样,逻辑不变。
- 验证:decode ret=0,输出 80 样本 PCM 非 0。

## 涉及文件
- `vendor/app_audio/app_audio_common.h`(新增 `LC3_CODEC_SR`,`LC3_WAVE_FORMAT` 改 INT24)
- `vendor/app_audio/app_audio_common.c`(编/解码 init 用 48k;保留已加的 `set_bitrate`)
- `vendor/app_audio/app_audio_1t4r_uni_tx.c`(重采样+48k 编码+缓冲)
- `vendor/app_audio/app_audio_1t4r_uni_rx.c`(48k 解码+重采样+缓冲+修类型)

## 风险与验证点
- 重采样符号可链(LTO slim,链接时解析)——Stage 1 链接确认。
- 重采样输出数 == 240/80(3:1 整数比)——检查返回值,偏差则做样本缓冲对齐。
- 48k LC3 实例/scratch 缓冲足够——init throw 保护(注释显示 48k 尺寸)。
- 48kbps 在 48k 合法性——Stage 0 确认;若非法则码率/帧长/RF 需重议。

## 不在本次范围(你的调试代码,保持现状)
RX 的 `break` 单通道、UART 送编码流 vs 解码流的取舍、`enc_size` 宏命名、TX 无数据时 RF 重发旧帧等,均为既存调试行为,本次不动。
