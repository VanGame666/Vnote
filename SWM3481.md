# 单片机摄像头极简抽象架构（实用主义版）

## 1. 核心思想转变：放弃“动态万能”，拥抱“静态专用”

- **Linux V4L2** 为了解决“未知硬件、未知格式、多应用抢占”而设计，需要动态协商。
- **单片机** 解决的是“固定硬件、固定格式、单应用裸跑/RTOS”的场景。
- **结论**：砍掉所有运行时状态机（`QBUF/DQBUF`、`TRY_FMT`），只在初始化时配置一次，运行时仅提供“启动”和“获取图像”两个动作。

---

## 2. 架构分层（保持两层半结构）

| 层级 | 名称 | 具体内容 |
| :--- | :--- | :--- |
| **顶层** | 应用层（App） | 调用 `camera_init()` 和 `camera_get_frame()` 即可。 |
| **中间层（核心）** | 抽象管理层 | 定义统一的 `camera_drv_t` 结构体，包含唯一的 `init` 和 `capture` 函数指针。 |
| **底层** | 硬件驱动 | 细分为 **Sensor配置（I2C/SPI）** 和 **Host传输（DMA/CSI）** 两个独立模块。 |

---

## 3. 核心数据结构（编译期绑定）

与之前不同，缓冲区和配置参数全部定义为**全局常量或静态数组**，运行时不再申请释放。

~~~c
// camera_config.h
// 所有配置在编译期确定，无需运行时修改
typedef struct {
    uint32_t width;          // 如 320
    uint32_t height;         // 如 240
    uint32_t pixel_format;   // 如 CAM_FMT_JPEG
    uint8_t *frame_buffer;   // 指向外部SDRAM或内部大数组的指针
    uint32_t buffer_size;    // width * height * 2 等
} camera_config_t;

// 极简操作集（去掉 start/stop/set 等动态控制，只留核心）
typedef struct {
    // 1. 初始化：传入固定配置，内部初始化I2C、DMA、Sensor寄存器
    int (*init)(const camera_config_t *cfg);
    
    // 2. 获取一帧图像：阻塞等待DMA完成，返回图像指针
    int (*capture)(uint8_t **out_buf, uint32_t *out_len);
    
    // 3. （可选）反初始化，极少用到
    int (*deinit)(void);
} camera_drv_t;
~~~

---

## 4. 底层驱动实现（彻底分离 Sensor 和 Host）

将具体摄像头（如OV2640）的操作拆分为两个 `.c` 文件，但在上层通过一个总驱动文件组合。

~~~c
// sensor_ov2640.c - 只负责寄存器配置
static int ov2640_write_table(const uint8_t (*regs)[2], int len) {
    for (int i = 0; i < len; i++) {
        i2c_write(OV2640_ADDR, regs[i][0], regs[i][1]);
    }
    return 0;
}
int ov2640_init(const camera_config_t *cfg) {
    // 1. 软复位
    // 2. 加载初始化寄存器数组（硬编码的静态表）
    // 3. 根据 cfg->width/height 设置输出尺寸
    return 0;
}

// host_stm32_dcmi.c - 只负责MCU外设
static uint8_t *current_dma_buf = NULL;
static uint32_t buf_len = 0;
static volatile uint8_t capture_done = 0;

void DCMI_DMA_IRQHandler(void) {
    capture_done = 1;  // 简单标志，不维护复杂队列
}

int host_start_capture(uint8_t *buf, uint32_t len) {
    current_dma_buf = buf;
    buf_len = len;
    capture_done = 0;
    HAL_DCMI_Start_DMA(&hdcmi, DCMI_MODE_CONTINUOUS, (uint32_t)buf, len/2); // 假设2字节像素
    return 0;
}

int host_wait_for_frame(uint32_t timeout_ms) {
    while(!capture_done) { /* 阻塞或延时 */ }
    capture_done = 0; // 手动清零，准备下一帧
    return 0;
}
~~~

---

## 5. 极简的“摄像头安装”机制（去除非必要的函数指针调用）

不再搞复杂的 `camera_install` 动态注册。由于单片机程序是静态编译的，直接用**条件编译（宏）**或**弱函数重定向**来更换摄像头。

### 方案一：推荐做法（直接替换底层驱动文件）
维护两套驱动文件，构建时通过工程文件（Makefile/Keil Project）选择包含哪个文件。

- `camera_drv_ov2640.c`
- `camera_drv_gc032a.c`

它们实现完全相同的顶层函数：
~~~c
// camera_drv_ov2640.c
int camera_init(const camera_config_t *cfg) {
    // 调用 ov2640_init 和 host_init
}
int camera_capture(uint8_t **out_buf, uint32_t *out_len) {
    // 调用 host_start_capture 和 host_wait_for_frame
}
~~~

上层代码完全无需改动，这是单片机最常用的“换文件即换硬件”策略，零性能开销。

### 方案二：若必须运行时切换（极少见）
保留一个单一的函数指针表：
~~~c
// camera_abstract.c
static const camera_drv_t *active_drv = NULL;

void camera_bind(const camera_drv_t *drv) {
    active_drv = drv;
}

// 在 main 初始化时调用 camera_bind(&ov2640_drv);
// 但绝大多数单片机项目不需要这个，因为不可能运行时热插拔摄像头。
~~~

---

## 6. 致命的简化：消除“缓冲队列（VB2）”

在Linux中，QBUF/DQBUF是为了解决 **“DMA填满后，应用没及时取走”** 的高并发竞争问题。

单片机做法：
1. **如果使用双缓冲（Ping-Pong DMA）**：
   - 定义两个全局数组 `buf1` 和 `buf2`。
   - 在 DMA 半满/全满中断中，通过 `current_dma_buf` 指针切换指向 `buf1` 或 `buf2`。
   - 应用调用 `camera_capture` 时，**直接返回当前正在被DMA使用的“上一个”完整帧指针**。
   - **不需要入队/出队状态机**，因为应用是唯一的消费者，且DMA传输和CPU处理是严格交替的（乒乓机制天然同步）。

2. **如果使用单缓冲（简单场景）**：
   - 应用调用 `camera_capture` 时，手动启动DMA，等待传输完成中断，处理完再启动下一次。
   - 整个过程是串行的，完全不需要队列。

~~~c
// 极简双缓冲捕获实现（无状态队列）
static uint8_t fb[2][BUFFER_SIZE];
static int active_index = 0;
static volatile int ready_index = -1;

void DMA_IRQ_Callback(void) {
    // DMA传输完成，切换活跃缓冲区
    ready_index = active_index;
    active_index = (active_index + 1) % 2;
    // 重启DMA指向 fb[active_index]
}

int camera_capture(uint8_t **out_buf, uint32_t *out_len) {
    if (ready_index == -1) return -1; // 没有完整帧
    *out_buf = fb[ready_index];
    *out_len = BUFFER_SIZE;
    ready_index = -1; // 消费完毕，交给应用处理，不再维护复杂状态
    return 0;
}
~~~

---

## 7. 更换摄像头的实际操作清单

如果你要换一个摄像头，物理上你需要做的是：

1. **拷贝一份驱动模板**（如 `sensor_newchip.c`）。
2. **填入该芯片的I2C寄存器初始化数组**（数据手册提供）。
3. **填入 `set_resolution` 对应的寄存器配置**。
4. **顶层调用 `camera_init` 时，传入相同的 `camera_config_t` 结构体**（宽高保持不变，应用层代码零改动）。

**不需要改动的部分**：
- 应用层调用逻辑（`camera_capture` 循环）。
- DMA配置代码（只要像素格式不变）。
- 顶层头文件接口。

---

## 8. 总结：思想降维

| Linux V4L2 做法 | 单片机极简替换 |
| :--- | :--- |
| 运行时 `open/close` | 编译期全局对象，上电即存在 |
| 动态 `REQBUFS` 申请内存 | 全局静态数组 `uint8_t fb[2][100KB]` |
| `QBUF/DQBUF` 队列管理 | DMA中断切换 `active_index`，应用直接读 `ready_index` |
| `TRY_FMT/S_FMT` 格式协商 | 初始化时硬编码宽高，失败则 `assert` 死机（永不失败） |
| 复杂的 `media_ctl` 拓扑 | 直接按顺序调用 `i2c_init()` -> `sensor_init()` -> `dma_init()` |

**终极结论**：单片机摄像头抽象，不需要“万能适配器”，只需要“标准接口插座”。把不确定性（硬件寄存器）封装在底层，把确定性（DMA缓冲、分辨率）暴露为静态配置，这就是最优雅、最高效的单片机架构。